#include<iostream>
using namespace std;
void maxHeap(int A[],int i){
    int left = 2*i;
    int right = 2*i+1;
    int largest;
    if(left<=A.heapsize){}

}

int main(){
    int A[]={1,6,8,4,5,2,0,3};
    int size = sizeof(A)/sizeof(A[0]);
    maxHeap(A,0);
    for(int i =0;i<=size;i++);
       cout<<A[i];
    return 0;
}